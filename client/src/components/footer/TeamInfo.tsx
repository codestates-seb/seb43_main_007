import styled from "styled-components";

function TeamInfo() {
   return <TeamInfoContainer>Team. 메인플스토리</TeamInfoContainer>;
}

export default TeamInfo;

const TeamInfoContainer = styled.div`
   display: flex;
`;
